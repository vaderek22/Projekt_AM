import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Review = () => {
  return (
        <Text>Ekran recenzje</Text>
  );
};

const styles = StyleSheet.create({
  center: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
});

export default Review;
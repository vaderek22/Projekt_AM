import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Fav = () => {
  return (
        <Text>Ekran ulubione</Text>
  );
};

const styles = StyleSheet.create({
  center: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
});

export default Fav;